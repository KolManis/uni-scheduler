# TODO

## Экзамены и сессии
Модель данных сейчас про недельное расписание, экзаменов нет. Что нужно уточнить и сделать:

- Отдельная сущность `Exam` или отдельный `ClassType = "exam"` в существующих планах?
- Расписание должно быть привязано к семестру с конкретной датой начала/конца?
- Экзамены попадают в то же расписание что и пары, или в отдельное «расписание сессии»?
- Как учитывать межэкзаменные интервалы (минимум N дней между экзаменами у одной группы)?
- Продолжительность экзамена и его окно в дне (обычно один экзамен = целый день или полдня)?

## HC7 в PatchAssignment
`PatchAssignment` проверяет HC1–HC3 (занятость), но не проверяет HC7 (недоступность
преподавателя). При ручном переносе занятия можно поставить его в слот, помеченный преподавателем
как недоступный. Аналогично тому, что было исправлено в `LocalSearch`, — дыра в проверке
слот-зависимых ограничений (см. SOLVER-003).
