-- =============================================================
-- КАФЕДРЫ (departments)
-- id   — идентификатор (используется в teachers и subject_plans)
-- name — название кафедры
-- =============================================================

INSERT INTO departments (id, name) VALUES
  ('itas', 'ИТАС')
-- добавь строки по образцу ↑ если несколько кафедр
ON CONFLICT (id) DO NOTHING;
